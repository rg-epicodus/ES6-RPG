//Player Function
export class Player {
  constructor(attack, health) {
    this.attack = attack;
    this.health = health;
  }

  checkIfPlayerDead() {
    if (player.health <= 0) {
      let playerDead = true;
  }
}
  checkIfOpponentDead() {
    if (opponent.health <= 0) {
      let opponentDead = true;
  }
}
}
